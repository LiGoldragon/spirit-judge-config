;; Test material only. Diagnostics are non-load-bearing and redacted by default.
[
  (Case clear-verdict None)
  (Case private-testimony (Redacted [hash-or-summary-only]))
  (Case provider-error (Redacted [no credential values]))
]
