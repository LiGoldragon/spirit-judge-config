# Agent guidance — spirit-judge-config

Read `ARCHITECTURE.md` before editing.

Keep prompt prose public, non-secret, and organized as Markdown by judge concern.
Use NOTA for manifests and fixtures. Do not add JSON, YAML, CSV, provider
credentials, private data, Rust binaries, or Nix-built prompt copies here.

Use judge terminology for new names. The old source paths may be quoted only as
provenance for migrated material.
